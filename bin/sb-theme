#!/bin/sh
# Utility functions for theming statusbar scripts.

dark="#424242"
light="#535353"
warning="#ffd218"
critical="#f44336"
foreground="#FFFFFF"

echo="echo"

display_noline() {
    echo="echo -n"
    display "$@"
}

display() {
    if [ -n "$2" ]; then
        background="$2"
        if [ "$background" = "$warning" ]; then
            foreground="#000000"
        fi
    else
        case "$(basename "$0")" in
            sb-music) background=$light ;;
            sb-network) background=$dark ;;
            sb-disk) background=$light ;;
            sb-memory) background=$dark ;;
            sb-loadavg) background=$light ;;
            sb-mic) background=$dark ;;
            sb-record) background=$dark ;;
            sb-volume) background=$dark ;;
            sb-battery) background=$light ;;
            sb-cal) background=$dark ;;
            sb-date) background="#F2F3F4"; foreground=$dark;;
            *) background="#FFFFFF" ;;
        esac
    fi


    case "$STATUSBAR" in
        dwmblocks) $echo "^b$background^^c$foreground^  $1  " ;;
        *) $echo "$1" ;;
    esac
}


